void main(){
  // valur cannot be changed once it's assigned
  // values are known at runtime
  final videoTitle = "Flutter for beginners";
  print(videoTitle);

  final dateAndTime= DateTime.now();
  print(dateAndTime);
}