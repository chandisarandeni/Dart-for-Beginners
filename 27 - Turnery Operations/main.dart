void main() {
  // Declare a boolean variable to indicate if it's the weekend
  bool isWeekend = true;

  // Use a ternary operator to print a message based on the value of isWeekend
  // If isWeekend is true, it prints "Today is a weekend"
  // Otherwise, it prints "Today is a weekday"
  // ignore: dead_code
  print(isWeekend ? "Today is a weekend" : "Today is a weekday");
}